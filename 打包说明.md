# Theo文件传输 · 打包发布说明

## 一、环境要求

- Windows 10/11
- .NET SDK 10（命令行运行 `dotnet --list-sdks` 能看到 10.x 即可）
- 下载地址：https://dotnet.microsoft.com/download （选 .NET SDK x64）

## 二、一条命令打包（推荐）

在**项目根目录**（即 TheoTransfer.csproj 所在目录）打开终端，执行：

```
dotnet publish TheoTransfer.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:EnableCompressionInSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -o ..\publish
```

参数说明：
| 参数 | 作用 |
|---|---|
| `-c Release` | 发布优化版 |
| `-r win-x64` | 目标平台 64 位 Windows |
| `--self-contained true` | 内置 .NET 运行时，目标电脑**无需安装任何环境** |
| `-p:PublishSingleFile=true` | 打成单个 exe |
| `-p:EnableCompressionInSingleFile=true` | 压缩体积（约 70MB） |
| `-p:IncludeNativeLibrariesForSelfExtract=true` | 原生库也打进单文件 |
| `-o ..\publish` | 输出到上级 publish 文件夹 |

完成后 `..\publish\` 里只有一个 **TheoTransfer.exe**（约 70MB），这就是可分享的成品。

## 三、打包前可做的事

### 1. 放图标（可选）
把 `logo.png` / `logo.ico` / `app.ico` 放进 `Assets\` 文件夹，详见 `Assets\图标说明.txt`。
其中 `app.ico`（exe 文件图标）已经在 `TheoTransfer.csproj` 中启用（`<ApplicationIcon>Assets\app.ico</ApplicationIcon>`），替换 app.ico 后重新发布即可。

### 2. 改版本号（可选）
`TheoTransfer.csproj` 中的 `<Version>1.6.1</Version>`。

## 四、发布产物如何分发

publish 文件夹里保留这两样即可分享：
- `TheoTransfer.exe` —— 软件本体，双击即用，绿色免安装
- `使用说明.txt` —— 给接收方看的使用指引

直接把 exe 发给对方（微信传文件助手、网盘、U盘均可）。
对方：双击运行 → 防火墙弹窗点「允许」→ 手机扫码点「连接」即可互传。

> 注意：exe 是自包含单文件，首次双击会解压原生库到临时目录，启动约慢 2~3 秒，属正常现象。

## 五、常见问题

- **提示找不到 dotnet 命令**：安装 .NET SDK 10 后重开终端。
- **发布时报 QRCoder 还原失败**：先执行 `dotnet restore TheoTransfer.csproj` 再发布。
- **改了代码想重新打包**：重复执行第二步的命令即可，会覆盖旧产物。
- **想减小体积**：把 `--self-contained true` 改为 false（体积约 2MB），但要求目标电脑已安装 .NET 10 桌面运行时，不推荐分享场景使用。

## 六、项目结构速览

```
TheoTransfer\
├─ TheoTransfer.csproj         项目文件（名称、版本、图标配置）
├─ App.xaml / App.xaml.cs      应用入口
├─ MainWindow.xaml(.cs)        主界面（模式切换、二维码、传输记录）
├─ Assets\                     图标（logo.ico / app.ico，编译时内嵌）
├─ Core\
│  ├─ WebUI.html               手机端网页（内嵌进 exe，含安全提醒）
│  ├─ TransferServer.cs        HTTP 服务（上传/下载/鉴权）
│  ├─ AppCore.cs               配对码、静态密钥、会话、上传会话管理
│  ├─ AppSettings.cs           设置持久化（%APPDATA%\TheoTransfer）
│  ├─ TransferRecord.cs        传输记录模型
│  ├─ UploadSession.cs         上传会话（断点续传）
│  └─ SharedFile.cs            共享文件模型
└─ ..\publish\                 发布输出（dotnet publish 后生成）
```
